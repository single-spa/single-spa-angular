{
  "$schema": "http://json-schema.org/schema",
  "id": "single-spa-ng-add",
  "title": "single-spa ng-add schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project to update",
      "$default": {
        "$source": "projectName"
      }
    },
    "routing": {
      "type": "boolean",
      "description": "When true, angular application supports routing",
      "default": true,
      "x-prompt": "Does your current application use Angular routing?"
    }
  },
  "required": []
}
