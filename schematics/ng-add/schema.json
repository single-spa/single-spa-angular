{
  "$schema": "http://json-schema.org/schema",
  "$id": "single-spa-ng-add",
  "title": "single-spa ng-add schematic",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "Name of the project to update",
      "$default": {
        "$source": "projectName"
      }
    },
    "routing": {
      "type": "boolean",
      "description": "When true, angular application supports routing",
      "default": true,
      "x-prompt": "Does your application use Angular routing?"
    },
    "port": {
      "type": "number",
      "description": "Port to listen on",
      "default": 4200,
      "x-prompt": "What port should your project run on?"
    }
  },
  "required": []
}
